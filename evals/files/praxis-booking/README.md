# Fixture: Praxis-Terminanfrage

Only the booking page of a fictional aesthetic-surgery practice site. Everything else about the site is unknown to the executor — that is the point: the eval checks whether the skill recognises Art. 9 data from the *fields*, treats Formspree and Calendly as US processors in the health-data path, and asks the intake questions instead of writing a generic policy.

Invented practice, invented URLs. `xEXAMPLE` is not a real Formspree form.
